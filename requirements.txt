# Full dependencies for RAG (telegram_bot.py with ChromaDB)
# Optimized for Railway deployment with CPU-only PyTorch

# CRITICAL: CPU-only PyTorch index MUST be first to avoid 3GB CUDA packages
--extra-index-url https://download.pytorch.org/whl/cpu

# Install torch FIRST with explicit CPU-only version
torch==2.10.0+cpu

# Core telegram bot
python-telegram-bot[job-queue]>=20.0
python-dotenv>=1.0.0

# Google Drive download utility
gdown>=4.7.0

# LangChain and AI
langchain>=0.1.0
langchain-community>=0.1.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.1.0

# Vector database and embeddings (will use CPU-only torch)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Timezone support for Ethiopian time
pytz>=2023.0

# Optional but recommended
streamlit>=1.28.0  # For test_rag_ui.py
